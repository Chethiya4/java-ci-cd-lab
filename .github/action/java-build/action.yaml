name: "Java Build Action"

description : "Compile and run Java Program"

runs:
   using : "composite"

steps:
    - name : Compile Java
      run : Javac Main.java 
      shell:branches

    - name:Run Java
      run : Java Main
      shell: bash    