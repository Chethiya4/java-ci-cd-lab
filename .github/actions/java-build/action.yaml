name: "Build Status Action"
description: "Returns build result"

outputs:
 result:
  description: "Build result"

runs:
  using: "composite"

  steps:
    - name: Set Output
      run: echo "result=Build Successful" >> $GITHUIB_OUTPUT
      shell: bash

    